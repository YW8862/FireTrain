# FireTrain灭火器实操测评系统前端美化需求文档

## 1. 文档概述

### 1.1 文档目的

本文档用于指导 FireTrain 灭火器实操测评系统前端页面的统一美化与交互优化工作。目标不是推翻现有结构，而是在保留当前业务流程、路由结构和技术栈的前提下，完成一套可执行、可验收、可拆分开发任务的前端改版规范。

### 1.2 本期改版目标

- 去 AI 味：弱化科技感视觉、术语和反馈方式，将界面重点从“AI 识别过程”转向“实操训练引导与结果呈现”。
- 贴消防主题：整体风格采用“工业级消防培训”方向，强调规范、安全、严谨、易读。
- 提升美观与一致性：统一色彩、字号、间距、组件状态和图表风格，减少当前页面视觉割裂。
- 提升交互体验：补齐加载、空状态、异常状态、反馈提示、表格筛选、视频操作等细节。
- 保证低成本落地：继续基于 Vue3、Element Plus、ECharts 和现有页面结构实现，不新增复杂依赖。

### 1.3 本期范围

本期范围覆盖项目全部前端页面，包括普通用户端、训练报告页和管理端页面。

#### 页面清单与当前实现映射

| 角色 | 路由 | 页面名称 | 当前文件 |
| --- | --- | --- | --- |
| 公共 | `/login` | 登录页 | `frontend/src/views/LoginView.vue` |
| 公共 | `/register` | 注册页 | `frontend/src/views/RegisterView.vue` |
| 用户 | `/` | 首页 | `frontend/src/views/HomeView.vue` |
| 用户 | `/training` | 实操训练页 | `frontend/src/views/TrainingView.vue` |
| 用户 | `/history` | 训练历史页 | `frontend/src/views/HistoryView.vue` |
| 用户 | `/stats` | 训练统计页 | `frontend/src/views/StatsView.vue` |
| 用户 | `/profile` | 个人中心页 | `frontend/src/views/ProfileView.vue` |
| 用户 | `/report/:id` | 用户训练报告页 | `frontend/src/views/ReportView.vue` |
| 用户通用 | 多页 | 顶部导航 | `frontend/src/components/NavBar.vue` |
| 管理端 | `/admin` | 后台布局容器 | `frontend/src/views/admin/AdminLayout.vue` |
| 管理端 | `/admin/dashboard` | 仪表盘 | `frontend/src/views/admin/Dashboard.vue` |
| 管理端 | `/admin/users` | 用户管理 | `frontend/src/views/admin/UserManagement.vue` |
| 管理端 | `/admin/users/new` | 新增用户 | `frontend/src/views/admin/UserDetailView.vue` |
| 管理端 | `/admin/users/:id` | 用户详情 | `frontend/src/views/admin/UserDetailView.vue` |
| 管理端 | `/admin/trainings` | 训练数据管理 | `frontend/src/views/admin/TrainingManagement.vue` |
| 管理端 | `/admin/video-upload` | 管理员视频检测 | `frontend/src/views/admin/AdminVideoUpload.vue` |
| 管理端 | `/admin/logs` | 操作日志 | `frontend/src/views/admin/OperationLogs.vue` |
| 管理端 | `/admin/admins` | 管理员管理 | `frontend/src/views/admin/AdminManagement.vue` |
| 管理端 | `/admin/report/:id` | 管理端训练报告页 | `frontend/src/views/admin/AdminReportView.vue` |

### 1.4 本期不做事项

- 不修改前后端核心业务流程，不改变训练创建、录制、上传、评分、报告查看的主链路。
- 不在本期引入新的复杂 UI 框架、播放器框架或 3D 可视化依赖。
- 不重构普通用户端导航结构为左侧栏；普通用户端继续使用顶部导航。
- 不推翻管理端现有“顶部栏 + 左侧菜单 + 主内容区”布局模型，只做视觉升级与体验统一。

### 1.5 技术约束

- 技术栈保持为 Vue3 + Element Plus + ECharts。
- 视频回放优先使用浏览器原生 `video` 能力和现有组件体系实现，不要求引入 `video.js`。
- 设计规范优先通过全局样式变量、Element Plus 主题覆盖和业务组件样式封装落地。

## 2. 设计原则与禁用项

### 2.1 风格原则

采用“工业级消防培训风格”，核心关键词为：严谨、清晰、实操导向、专业、安全。整体视觉应减少炫技感和装饰性，突出训练信息、步骤提示和结果反馈。

### 2.2 功能优先原则

所有美化调整必须以不影响核心功能为前提，尤其不能遮挡以下区域：

- 视频预览与摄像头权限提示
- 训练开始、暂停、完成、取消等关键操作按钮
- 报告页得分与建议信息
- 管理端表格、筛选器、分页器和批量操作入口

### 2.3 分角色一致性原则

- 用户端与管理端共享同一套设计语言、颜色语义、字体体系、按钮规则和状态规范。
- 用户端与管理端允许使用不同导航容器。
- 用户端重点体现“训练指导与个人结果”；管理端重点体现“数据管理与查询效率”。

### 2.4 禁用项

以下效果和表达在本期统一禁止使用：

- 高饱和霓虹渐变、大面积紫蓝科技渐变
- 玻璃拟态、毛玻璃导航、强发光阴影
- 粒子、光效、扫描线、3D 骨架、神经网络、机器人等 AI 科技元素
- “AI 正在分析”“AI 智能评估”“模型加载中”等强 AI 话术
- 过度动画，如弹跳、旋转入场、强缩放、闪烁

## 3. 设计令牌

### 3.1 颜色规范

本项目采用“语义色 + 中性色阶”的方式定义颜色，不再使用“全局颜色不超过 5 种”的限制说法。

| 令牌 | 色值 | 用途 |
| --- | --- | --- |
| `color-primary` | `#1E40AF` | 用户端主按钮、导航选中态、重点标题、图表主色 |
| `color-primary-hover` | `#1A3699` | 主按钮 hover、顶部导航 hover 背景 |
| `color-danger` | `#D92121` | 关键警示、错误反馈、危险按钮、当前训练风险提示 |
| `color-danger-hover` | `#C01919` | 危险按钮 hover |
| `color-success` | `#10B981` | 成功反馈、优秀标签、完成状态 |
| `color-warning` | `#D97706` | 警告提示、需调整状态、待确认反馈 |
| `color-bg-page` | `#F8F9FA` | 页面背景 |
| `color-bg-card` | `#FFFFFF` | 卡片、弹窗、表单容器背景 |
| `color-bg-soft` | `#F3F4F6` | 次级背景、说明条、骨架背景 |
| `color-border` | `#E5E7EB` | 默认边框、分割线 |
| `color-border-strong` | `#CBD5E1` | 强调边框、模块分组边界 |
| `color-text-primary` | `#1F2937` | 主标题、正文主文字 |
| `color-text-secondary` | `#4B5563` | 普通说明文字 |
| `color-text-tertiary` | `#6B7280` | 辅助文字、时间、占位说明 |
| `color-disabled` | `#C9CDD4` | 禁用按钮、禁用标签、不可用状态 |
| `color-black-video` | `#111827` | 视频容器背景 |

补充要求：

- 页面背景禁止使用紫蓝渐变作为主背景。
- 背景纹理如需使用，只允许低透明度消防线稿，透明度不高于 8%。
- 图表仅允许使用主色、成功色、警示色和中性色，不使用荧光色。

### 3.2 字体与字号规范

- 字体：`Inter`、`Microsoft YaHei`、系统无衬线字体。
- 视觉稿标注统一使用 `px`；代码实现可按 `1rem = 16px` 换算，以便响应式适配。
- 字号层级：

| 层级 | 大小 | 字重 | 用途 |
| --- | --- | --- | --- |
| 页面主标题 | 24px | 600 | 页面标题、训练报告标题 |
| 模块标题 | 20px | 600 | 卡片主标题、板块标题 |
| 区块标题 | 18px | 600 | 小节、面板标题 |
| 正文 | 14px | 400 | 表格内容、说明文案、操作提示 |
| 小字说明 | 12px | 400 | 时间、补充说明、表单提示 |
| 数值强调 | 24px-40px | 700 | 得分、次数、关键统计指标 |

规则补充：

- 页面标题和模块标题默认左对齐。
- 数值类内容居中或右侧强调展示，说明类内容左对齐。
- 单页中不超过 4 个明显字号层级。

### 3.3 间距规范

统一采用 8px 基准体系：

- 页面主容器内边距：24px
- 卡片间距：16px
- 组件间距：8px
- 区块间距：24px
- 表单项上下间距：16px
- 按钮组间距：8px
- 表格工具栏与表格主体间距：16px

### 3.4 圆角、边框与阴影

| 项目 | 规范 |
| --- | --- |
| 卡片圆角 | 12px |
| 输入框/按钮圆角 | 6px |
| 小标签圆角 | 4px |
| 默认边框 | 1px solid `color-border` |
| 强调边框 | 1px solid `color-border-strong` 或 1px solid `color-danger` |
| 卡片阴影 | `0 2px 12px rgba(15, 23, 42, 0.06)` |
| hover 阴影 | `0 6px 20px rgba(15, 23, 42, 0.08)` |

规则补充：

- 不使用高强度发光阴影。
- 不使用大圆角胶囊化风格。
- 阴影应仅用于层级区分，不用于装饰炫技。

### 3.5 图标与插图规范

- 图标风格统一为扁平化、线性、安全培训风格。
- 文档中的 emoji 仅作说明示意，实际实现统一替换为 Element Plus 图标或 iconfont/本地图标资源。
- 优先使用以下视觉语义：
  - 灭火器/培训：训练入口、步骤说明
  - 摄像头/回放：视频相关动作
  - 台账/文档：历史记录、管理列表
  - 图表/统计：数据分析
  - 警示/对勾/关闭：状态反馈
- 插图只允许用于首页步骤说明、训练页步骤引导、安全提示弹窗等低频区域，不强制所有页面使用插图。

### 3.6 图表规范

- 折线图主线色：`color-primary`
- 柱状图主色：`color-primary`，对异常项可使用 `color-danger`
- 优秀数据：`color-success`
- 警示数据：`color-warning`
- 坐标轴、网格线、辅助线使用中性色
- Tooltip 文案统一使用“实操得分”“操作建议”“步骤表现”等术语，不出现 “AI 评分”

## 4. 导航与信息架构规范

### 4.1 普通用户端导航

普通用户端继续采用顶部导航，不改为左侧侧边栏。

#### 用户端导航样式要求

- 背景：纯白或浅灰白，不使用毛玻璃和半透明模糊。
- 高度：64px 左右。
- 左侧：系统标题 + 消防相关图标。
- 中部：主导航项，包括首页、训练、历史、统计。
- 右侧：用户头像、用户名、角色入口、个人中心、退出登录。
- 选中态：使用主色强调，可配底部 2px 主色线条或轻背景色块。
- hover：仅允许轻微底色变化和文字变色，不允许缩放抖动。

### 4.2 管理端导航

管理端沿用当前布局模型：顶部栏 + 左侧菜单 + 主内容区。

#### 管理端布局要求

- 顶部栏：显示系统名称、当前登录人、角色标识、角色切换入口、退出登录入口。
- 左侧菜单：保留现有后台菜单结构，不增减一级导航层级。
- 主内容区：保持数据管理页面的宽松留白和卡片承载结构。
- 当前菜单项：主色或深色背景突出，不使用 Element 默认蓝高亮。

### 4.3 页面容器规范

- 用户端页面最大内容宽度建议控制在 1200px 至 1440px 之间。
- 管理端内容区最大宽度建议控制在 1440px 至 1600px 之间。
- 报告页可使用较宽布局，但需保证关键得分模块首屏可见。
- 所有页面需避免“满屏漂浮卡片 + 大面积渐变背景”的旧样式。

## 5. 全局组件规范

### 5.1 按钮

| 类型 | 样式 | 场景 |
| --- | --- | --- |
| 主按钮 | 主色实心，白字，圆角 6px，hover 略加深 | 登录、注册、开始训练、确认操作 |
| 次按钮 | 白底描边，边框为 `color-border`，hover 变主色边框 | 返回、筛选、关闭、查看详情 |
| 危险按钮 | 危险色实心，hover 略加深 | 结束训练、删除、取消上传 |
| 成功按钮 | 成功色实心，仅在强语义场景使用 | 完成、恢复、确认已处理 |

规则：

- 常规按钮高度 36px 至 40px。
- 大按钮仅用于首页主入口、训练主流程按钮。
- 按钮图标与文字间距 8px。

### 5.2 输入框与筛选器

- 默认边框为 `color-border`。
- focus 态使用主色，不使用强发光蓝色阴影。
- placeholder 使用 `color-text-tertiary`。
- 多条件筛选场景优先将输入框、下拉、日期、按钮放在同一工具栏卡片中。

### 5.3 卡片

- 卡片作为主要内容承载容器，统一白底、12px 圆角、轻阴影。
- 标题区与内容区间距明确，避免内容直接贴边。
- 首屏统计卡片和训练引导卡片可使用浅色背景，但不使用彩色渐变填满整卡。

### 5.4 表格

- 表头背景使用浅灰。
- 行 hover 使用浅主色或浅警示色背景。
- 行高适中，避免数据过密。
- 分数字段、状态字段、关键操作列需有明显层级。
- 分页器统一底部对齐；管理端靠右，用户端可居中。

### 5.5 弹窗与抽屉

- 标题区左侧可配图标，但图标不应喧宾夺主。
- 弹窗宽度按场景控制，普通说明类 520px 左右，视频回放类 720px 至 960px。
- 弹窗底部按钮顺序为“次操作在左，主操作在右”。

### 5.6 Message、Alert、Notification

- 成功：绿色语义，文案强调“操作完成”“提交成功”“训练已完成”。
- 警告：橙色语义，文案强调“需确认”“需调整”“请检查”。
- 错误：红色语义，文案强调“失败”“无法继续”“请重试”。
- 信息：中性语义，用于“准备中”“上传中”“加载中”。

### 5.7 空状态、加载态、异常态

- 空状态必须给出“为什么为空”和“下一步做什么”。
- 列表、图表加载优先使用骨架屏或局部 loading，不使用全页长时间遮罩。
- 接口异常必须展示操作建议，如“刷新重试”“返回上一页”“检查网络连接”。

### 5.8 视频容器规范

- 统一使用 `16:9` 比例容器。
- 默认背景使用深色，不使用渐变。
- 摄像头未开启、视频不可播放、上传中、暂停中等状态应有明确遮罩说明。
- 风险状态可使用红色边框或顶部警示条，不使用闪烁描边。

### 5.9 训练流程状态矩阵

| 状态 | 文案 | 视觉表现 | 操作要求 |
| --- | --- | --- | --- |
| 未开始 | 训练准备中 | 中性卡片 + 说明清单 | 可开始训练 |
| 摄像头未开启 | 请开启摄像头 | 深色容器 + 主按钮 | 允许重新尝试 |
| 训练中 | 正在训练 | 正常视频边框 + 顶部状态标签 | 允许暂停、完成、取消 |
| 已暂停 | 训练已暂停 | 半透明遮罩 + 恢复说明 | 允许继续或取消 |
| 上传中 | 正在上传训练视频 | 进度条 + 文本 | 禁止重复提交 |
| 评分中 | 正在生成测评结果 | 局部 loading + 中性说明 | 禁止重复完成 |
| 成功完成 | 实操测评完成 | 成功提示 + 跳转报告 | 支持查看报告 |
| 失败/中断 | 本次训练未成功完成 | 危险提示 + 重试建议 | 支持重新训练 |

## 6. 文案规范与替换词表

### 6.1 文案原则

- 统一强调“训练”“实操”“规范”“测评”“建议”。
- 尽量描述用户动作和结果，不描述内部算法过程。
- 面向普通用户的页面不主动强调模型、识别、算法、LLM 等实现细节。

### 6.2 禁用词与推荐替换

| 禁用表达 | 推荐表达 |
| --- | --- |
| AI 智能评估 | 实操动作规范测评 |
| AI 分析 | 测评分析 / 结果分析 |
| AI 评分 | 实操得分 / 测评得分 |
| 模型加载中 | 测评系统准备中 |
| AI 快速检测 | 视频预检 / 训练预检 |
| 姿态识别 | 动作识别 / 动作检测 |
| 关键点识别错误 | 操作动作不规范 |
| AI 改进建议 | 训练改进建议 |
| AI 详细反馈 | 详细反馈 |

### 6.3 允许保留的技术性表达

以下场景可保留较弱的技术性说明，但不得作为页面主视觉标题：

- 管理端技术说明
- 日志或调试类信息
- 开发态调试输出
- 仅管理员可见的检测说明

## 7. 响应式与适配规范

### 7.1 断点定义

| 断点 | 范围 | 适配策略 |
| --- | --- | --- |
| 桌面端 | `>= 1200px` | 按主布局正常显示 |
| 平板端 | `768px - 1199px` | 双栏改单栏或上下布局，统计卡片改 2 列 |
| 移动端 | `< 768px` | 导航收拢，表格转纵向重点信息卡或横向滚动 |

### 7.2 用户端适配要求

- 顶部导航在移动端改为折叠菜单或简化菜单，不新增左侧栏。
- 首页左右分栏在平板端改为上下布局。
- 训练页视频区优先全宽展示，引导面板下移。
- 报告页多列结构在移动端按“总分 > 建议 > 步骤 > 图表”顺序纵向排列。

### 7.3 管理端适配要求

- 侧边栏在平板端可收缩。
- 复杂表格在平板及移动端允许横向滚动。
- 筛选区域允许换行，但操作按钮需保持就近可见。

## 8. 用户端页面规范

### 8.1 登录页

#### 页面目标

提供安全、清晰、有消防培训属性的登录入口。

#### 信息结构

- 标题区：系统名称、副标题
- 表单区：账号、密码、记住我、登录按钮
- 底部区：注册入口、安全提示

#### 视觉重点

- 背景使用浅灰底 + 极低透明度消防线稿，不再使用大面积紫蓝渐变。
- 登录卡片宽度控制在 420px 至 500px。
- 标题建议调整为“灭火器实操训练测评系统”。

#### 交互状态

- 登录中按钮显示 loading，禁止重复点击。
- 登录失败时使用明确错误提示，不用笼统“操作失败”。

#### 验收点

- 无科技感渐变背景
- 无默认 Element 蓝色主视觉
- 登录表单、按钮、提示条风格与注册页统一

### 8.2 注册页

#### 页面目标

提供与登录页一致的注册体验，强调训练账号创建。

#### 信息结构

- 标题区
- 表单区：账号、邮箱、密码、确认密码
- 底部区：返回登录、安全提示

#### 视觉重点

- 与登录页共享布局、配色和卡片样式。
- 文案建议使用“注册训练账号”“已有账号，返回登录”。

#### 交互状态

- 校验失败信息在字段旁即时展示。
- 注册成功后跳转登录页并显示成功提示。

#### 验收点

- 与登录页视觉规则一致
- 表单错误态、成功态、禁用态均统一

### 8.3 首页

#### 页面目标

让用户快速理解训练目标、操作步骤和核心入口。

#### 信息结构

- 顶部安全提示条
- 左侧或上方：灭火器规范操作步骤说明
- 右侧或下方：开始训练、查看记录、查看统计三类入口
- 底部：训练须知或安全说明

#### 视觉重点

- 删除“AI 智能评估”等科技感宣传语。
- 首页不再以营销式宣传卡片为主，而以“训练引导 + 入口”结构为主。

#### 交互状态

- 安全规范支持弹窗查看。
- 入口按钮应支持 hover 和明确按压反馈。

#### 验收点

- 首屏能清楚看到训练入口
- 首页文案统一为实操导向
- 统计入口在首页可直达

### 8.4 训练页

#### 页面目标

突出视频训练主流程、步骤引导和过程反馈，是本期改版优先级最高页面。

#### 信息结构

- 顶部：训练标题、当前状态、安全提示
- 主体左侧：视频预览与控制区域
- 主体右侧：步骤引导、当前要求、实时反馈、阶段得分
- 底部：步骤条和控制按钮

#### 视觉重点

- 保留现有“视频区 + 侧边步骤区”主结构，但视觉统一为工业化风格。
- 视频区不使用炫目边框和玻璃遮罩。
- 右侧面板重点展示“当前步骤要做什么”“当前哪里需要调整”。

#### 交互状态

- 训练开始前：显示训练说明与确认信息
- 摄像头开启失败：明确提示权限和重试动作
- 暂停：半透明遮罩 + 恢复说明
- 上传中：显示百分比、进度、速度或阶段描述
- 预检异常：提示“可能未检测到有效动作”，允许用户确认是否继续
- 完成后：进入报告页

#### 验收点

- 训练状态一眼可识别
- 所有 AI 强技术文案完成替换
- 视频区、按钮区、步骤区信息层级清晰
- 平板和移动端优先保证视频区可用性

### 8.5 历史记录页

#### 页面目标

帮助用户快速查询训练记录、得分结果和报告入口。

#### 信息结构

- 顶部标题 + 新建训练按钮
- 筛选工具栏
- 表格主体
- 分页器

#### 视觉重点

- 表格减少后台化生硬感，增强等级与分数的可读性。
- 若后端数据支持，优先补充“等级”“操作建议”列；如果当前接口暂不支持，可先保留现有字段并预留样式规范。

#### 交互状态

- 无记录时展示空状态和“去训练”按钮。
- 已完成记录突出“查看详情/查看报告”按钮。

#### 验收点

- 状态、分数、时间字段清楚可辨
- 表格筛选区、表格主体、分页器间距统一
- 空状态和异常状态有明确说明

### 8.6 统计分析页

#### 页面目标

以简洁图表和关键指标帮助用户理解训练表现趋势。

#### 信息结构

- 顶部：标题 + 时间范围控制
- 第一屏：4 个关键指标
- 第二屏：趋势图、步骤分布图、能力维度图
- 第三屏：最近训练记录或步骤表现分析

#### 视觉重点

- 图表以主色和中性色为主，不使用蓝绿紫多彩渐变。
- 关键指标卡片应统一尺寸和视觉重心。

#### 交互状态

- 图表无数据时显示明确空状态。
- 切换时间维度时优先局部刷新，不全页闪烁。

#### 验收点

- 统计卡片、图表卡片、表格卡片风格统一
- Tooltip 文案和标题无 AI 表述
- 移动端至少保证图表可滚动查看，不出现挤压变形

### 8.7 个人中心页

#### 页面目标

整合个人资料、训练概况和常用设置入口。

#### 信息结构

- 左侧或顶部：头像、用户名、角色、基础资料
- 右侧或下方：训练统计、最近训练记录、账号设置
- 底部：退出登录

#### 视觉重点

- 当前仅有基础信息表单，本期应提升为“资料 + 训练概况”的组合页。
- 页面风格与统计页、历史页保持一致。

#### 交互状态

- 用户信息加载中显示骨架或局部 loading。
- 设置类操作使用弹窗或侧边抽屉，避免跳出式体验。

#### 验收点

- 个人资料区与训练数据区信息分组清晰
- 退出登录按钮危险语义明确但不刺眼

### 8.8 用户训练报告页

#### 页面目标

帮助用户快速理解本次训练成绩、问题和改进方向。

#### 信息结构

- 顶部：标题、返回入口
- 首屏：总分、等级、训练时间
- 中部：改进建议、总体反馈、步骤评分、维度图表
- 可选：视频回放区

#### 视觉重点

- “总分”是首屏核心视觉。
- “建议”和“步骤评分”必须优先于复杂图表。
- 文案从“训练评分报告”优化为“实操测评报告”或“训练报告”。

#### 交互状态

- 无维度数据时用空状态替代图表，不显示空雷达图。
- 无建议时展示“本次表现较稳定，继续保持”类中性文案。

#### 验收点

- 用户不需要理解算法也能读懂报告
- 总分、等级、建议和步骤问题在首屏或首屏后紧邻区域可见

## 9. 管理端页面规范

### 9.1 管理端布局页

#### 页面目标

建立稳定、专业、偏数据管理的后台视觉基线。

#### 视觉重点

- 顶部栏和侧边栏采用深蓝 + 白底内容区组合。
- 菜单图标和文案统一，不使用默认后台蓝灰混搭。
- 主内容区背景使用浅灰，内部用白色卡片承载。

#### 验收点

- 顶栏、侧栏、主内容区层级清晰
- 当前菜单高亮规则统一
- 与用户端虽结构不同，但属于同一套品牌风格

### 9.2 仪表盘

#### 页面目标

用最少的信息让管理员了解系统运行概况。

#### 信息结构

- 顶部标题
- 统计卡片区
- 分布图/进度图区
- 快捷操作区

#### 视觉重点

- 删除彩色渐变图标块，改为统一图标底板和语义色点缀。
- 快捷入口按钮按照主/次按钮体系统一。

#### 验收点

- 数据卡片尺寸统一
- 标题、数值、辅助说明层级明确

### 9.3 用户管理与用户详情

#### 页面目标

提升管理员查找用户、查看详情、编辑用户时的效率与清晰度。

#### 规范要求

- 列表页采用“筛选区 + 表格 + 分页”三段结构。
- 用户详情页采用“基础信息卡片 + 操作区”结构。
- 状态、角色、启用情况等字段必须有统一标签样式。

### 9.4 训练数据管理

#### 页面目标

高效查询训练记录、查看报告和执行管理操作。

#### 规范要求

- 顶部筛选卡片保留现有查询结构，但统一输入框、下拉、日期和按钮样式。
- 表格中“分数”“状态”“查看报告”“删除”等关键列需重点突出。
- 删除操作使用危险按钮和二次确认弹窗。

#### 验收点

- 表格可读性高
- 筛选区不拥挤
- 查看报告入口明显

### 9.5 管理员视频检测页

#### 页面目标

让管理员上传视频、跟踪分析状态并查看结果。

#### 信息结构

- 上传表单
- 上传进度区
- 上传结果区
- 使用说明区

#### 视觉重点

- 上传流程强调“目标用户、视频文件、当前状态”三件事。
- 将“AI 分析”相关表述弱化为“视频分析”“结果生成”；如需保留技术字样，仅放在说明区，不放在主按钮和主标题。

#### 交互状态

- 上传中禁止重复选择文件
- 上传可取消
- 分析中支持轮询刷新状态
- 已完成后突出“查看报告”

#### 验收点

- 上传状态、分析状态、失败状态区分清楚
- 表单、进度、结果三区块视觉分层明确

### 9.6 操作日志页

#### 页面目标

强化日志查询效率和风险感知。

#### 规范要求

- 采用管理端标准筛选 + 表格布局。
- 风险类日志使用警示色标签。
- 时间、操作者、动作对象、操作结果字段需清楚区分。

### 9.7 管理员管理页

#### 页面目标

统一管理员账户维护风格，体现权限敏感性。

#### 规范要求

- 新增、编辑、删除、角色变更操作统一使用更严谨的表单和确认弹窗样式。
- Root 相关操作视觉上应有更明确的风险区分。

### 9.8 管理端训练报告页

#### 页面目标

帮助管理员从单条训练记录中快速判断问题、质量和处置方向。

#### 信息结构

- 面包屑
- 训练记录信息卡
- 总分与维度评分卡
- 维度图表与步骤评分
- 建议与详细反馈

#### 视觉重点

- 报告页应延续用户报告页的信息层级，但增加“记录信息”和“管理判断”视角。
- 标题、卡片和建议区统一使用“训练记录详情”“改进建议”“详细反馈”等非 AI 表达。

#### 验收点

- 用户信息、训练信息、总分、步骤问题在同页内层级清晰
- 不再使用“AI 改进建议”“AI 详细反馈”等标题

## 10. 统一状态规范

### 10.1 页面级状态

| 场景 | 显示规则 |
| --- | --- |
| 首次加载 | 使用骨架屏或局部 loading，不使用空白页 |
| 无数据 | 显示空状态插图/说明/主操作按钮 |
| 网络失败 | 显示错误提示 + 重试按钮 |
| 权限不足 | 显示无权限提示 + 返回入口 |
| 查询结果为空 | 保留筛选条件，并给出调整建议 |

### 10.2 表单级状态

| 场景 | 显示规则 |
| --- | --- |
| 默认 | 白底、灰边框 |
| 聚焦 | 主色描边 |
| 校验失败 | 危险色描边 + 字段错误提示 |
| 禁用 | 灰背景、灰文字、禁用鼠标态 |

### 10.3 视频与报告级状态

| 场景 | 显示规则 |
| --- | --- |
| 摄像头不可用 | 显示原因、授权建议、重试按钮 |
| 视频上传失败 | 显示失败原因和重新上传入口 |
| 报告暂无维度数据 | 隐藏对应图表，显示文字说明 |
| 视频不可播放 | 显示占位说明和下载/重试建议 |

## 11. 开发落地建议

### 11.1 推荐实施顺序

1. 建立全局设计令牌和 Element Plus 基础覆盖
2. 改造用户端顶部导航与全局基础组件
3. 改造用户端登录、首页、训练、历史、统计、个人中心、报告页
4. 改造管理端布局和后台列表页
5. 改造管理端视频检测页和管理端报告页
6. 统一补充空状态、异常状态、骨架屏和提示文案

### 11.2 推荐落地方式

- 颜色、字号、圆角、阴影优先抽成全局 CSS 变量。
- Element Plus 默认主题色、Tag、Message、Dialog、Table 等应统一覆盖，减少逐页硬编码。
- 图表主题建议统一封装基础 option，避免每页各自定义色。
- 文案替换应优先整理为统一词表，避免页面遗漏。

## 12. 验收标准

### 12.1 全局验收项

- 普通用户端保留顶部导航，管理端保留当前后台布局模型。
- 全站去除明显科技感渐变、毛玻璃、粒子、3D 骨架和 AI 术语主视觉。
- 主色、危险色、成功色、中性色使用符合本文档规定。
- 按钮、表单、卡片、表格、弹窗、图表风格统一。
- 桌面端、平板端、移动端无明显错位、溢出、不可点击问题。

### 12.2 用户端验收项

- 登录/注册：卡片清晰、提示明确、无旧版渐变背景。
- 首页：训练引导突出，核心入口可直达。
- 训练页：视频、步骤、反馈、按钮层级清晰。
- 历史页：记录查询与报告入口明确。
- 统计页：指标、图表、趋势表达统一。
- 个人中心：资料与训练概况整合。
- 报告页：总分、建议、步骤问题易读易懂。

### 12.3 管理端验收项

- 后台布局统一且具有工业化管理风格。
- 仪表盘数据卡片、表格、筛选器、操作按钮风格一致。
- 视频检测页上传与分析状态可清晰追踪。
- 管理端报告页具备足够的信息密度和清晰层级。

### 12.4 文案验收项

- 用户端主页面不出现“AI 智能评估”“AI 评分”“模型加载中”等表达。
- 管理端如需保留技术说明，仅出现在说明性区域，不作为主标题。
- 错误提示、成功提示、状态提示统一采用“动作 + 结果 + 建议”格式。

### 12.5 依赖与实现验收项

- 不新增复杂依赖。
- 新增样式应可复用，不允许大量页面内重复硬编码。
- 不因美化引入核心功能回归问题。