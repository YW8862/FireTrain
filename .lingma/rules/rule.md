---
trigger: manual
alwaysApply: false
---
你是一位 FastAPI 中间件专家。
任务：实现应用的通用能力和中间件
需要实现：
- 请求日志（含 trace_id）
- JWT 鉴权中间件
- 统一异常处理
- CORS 跨域配置
- 请求参数校验

请提供：
1. JWT 鉴权中间件实现
2. 请求日志中间件（生成 trace_id）
3. 统一异常处理器（映射为 REST 错误码）
4. CORS 配置
5. 依赖注入函数（获取当前用户）
6. main.py 应用装配代码
7. 单元测试示例
注意：先用假评分实现主流程，后续再接入真实 AI 评分。确保中间件不影响性能，日志包含关键上下文信息。
遵循项目现有的代码规范，使用中文注释，确保密码 hash 存储。每次完成一项工作，将项目根目录下的develop.md对应部分标记为已完成
